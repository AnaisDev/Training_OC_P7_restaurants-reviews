$sizeMap = 500px;

#map {
  height: $sizeMap = 500px;
  background-color: $lightGray;
  width: $sizeMap = 500px;
  position: relative;
  border-radius: 15px;
  box-shadow: 0 0 5px gray;
}

@media screen and (max-width: 1000px) {
  #map {
    max-width: 100vw;
  }
}
